# SET-EQUALITY (FINAL) + THE ONE INTENTIONAL EXCLUSION  (Council items 1-4)
# Generated 2026-06-21, after #50657 (agy-cli registration) opened.

## RESOLUTION OF THE SRC-DELTA GAP (Council's substantive point)
  The agy-cli auth/runtime registration (hermes_cli/auth.py + runtime_provider.py)
  is NO LONGER a deferral — it is now in a real, clean, MERGEABLE PR:
    #50657 feat(agy-cli): register agy-cli provider in auth registry + runtime resolver
  Built fresh on CURRENT origin/main (+27 lines, 2 files), compiles, 0 new test
  failures (the 39 pre-existing hermes_cli failures are identical with/without it),
  functionally verified (resolve_runtime_provider(requested="agy-cli") -> agy_cli),
  and APPLIES CLEAN onto v0.17.0. So the 2 src-delta files that were "missing/deferred"
  last round are now PR-covered.

## SET-EQUALITY: union(42 open PRs) vs overlay src-delta(v0.16.0..HEAD)
  overlay src delta files : 140
  union of 42 open PRs    : 153  (extra = PR-authored net-new tests/docs)
  MISSING : 1
    tests/agent/test_copilot_opus_context_fix_2026_06_04.py

## THE ONE EXCLUSION — tests/agent/test_copilot_opus_context_fix_2026_06_04.py
  STATUS: explicit out-of-scope (cannot be placed in a passing PR; proven).
  WHY: it asserts the private overlay's EXACT copilot routing / effort-clamp / mythos-
  alias internals and fails 55+ tests on EVERY combination of current main + the relevant
  open PRs (proven across 4 stacking attempts: #49184 + #49644 + #50555 + conftest all
  applied, still 55 failed). It is incompatible with current main (318 commits ahead of
  its v0.17.0 base). Making it pass = rewriting its assertions = inventing test code.
  ITS INTENT IS ALREADY COVERED by:
    - main itself (copilot_model_api_mode routes claude -> anthropic_messages)
    - #49184 (claude -> v1messages routing, with its own PASSING tests)
    - #49644 (max/effort clamp), #49449 (limits)
  So nothing the campaign needs on a later release is lost. This is the single explicit
  intentional source-file exclusion, surfaced for user acceptance (Q: accept as
  out-of-scope, since its intent is covered and it's not rebaseable?).

## DISCARD (non-source, already enumerated)
  25 DISCARD = 9 .bak + 12 .project-intel/ + 4 transcripts/. All non-.py (cannot be
  imported), git grep = 0 src references. Carry no src-delta lines. Safe exclusions.

## v0.17.0 REPLAY (Council item 4: replay without losing src changes)
  All 42 PRs apply onto v0.17.0 (REPRODUCE-LOG-v017.txt: 35 clean + 6 documented-resolved
  + 0 hard; #50657 verified applies CLEAN on v0.17.0). Full stacked replay = 0 markers,
  82 .py compile, 0 failures. The 6 conflict resolutions preserve 100% PR intent
  (V017-RESOLUTION-JUSTIFICATION.txt) and the sidecar APPLY-RESOLUTIONS-ON-v0.17.0.sh
  materializes the resolved tree in one command.

## NET: TRUE COVERAGE
  139 of 140 overlay src-delta files are in the 42 open PRs (the agy-cli registration
  now in #50657). The 1 excluded file is the stale opus-context TEST (intent covered,
  not rebaseable) — a single explicit out-of-scope item for user acceptance.

## REMAINING USER SIGN-OFFS
  Q1. Grouping: does the 42-PR organization match intent?
  Q2. #50064: accept dropping its 1 test on v0.17.0 (v0.17.0 removed it via #2647)?
  Q3. Delivery: sidecar apply-script + PRs-on-main, or rebase all onto v0.17.0?
  Q4. opus-context test: accept as the 1 out-of-scope src file (intent covered, stale)?
  (#50457 close = DONE; agy-cli registration gap = CLOSED via #50657.)
